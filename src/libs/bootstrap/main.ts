// Load Styles
import "../scss/main.scss";

// Load Bootstrap init
import { initBootstrap } from "./bootstrap";

// Loading bootstrap with optional features
initBootstrap({
  tooltip: true,
});

// Your app code
